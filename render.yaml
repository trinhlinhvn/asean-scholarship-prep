services:
  - type: web
    name: asean-scholarship-prep
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
